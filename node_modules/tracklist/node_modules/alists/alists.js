exports.toObj = function (xs) {
    return xs.reduce( function(acc, x) {
        acc[x[0]] = x[1];
        return acc;
    }, {});
}

exports.toList = function(obj) {
    return Object.keys(obj).reduce( function(acc, k) {
        return acc.concat([[ k, obj[k] ]]);
    }, []);
}
